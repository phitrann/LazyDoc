# Optional. Leave empty to use the built-in Next.js proxy route at /api/research.
NEXT_PUBLIC_API_BASE_URL=

# Used by the Next.js proxy route to reach FastAPI from the frontend server.
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
